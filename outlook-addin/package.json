{
  "name": "desk-outlook-addin",
  "version": "1.0.0",
  "description": "Outlook add-in for opening emails in Desk",
  "private": true,
  "scripts": {
    "start": "npx office-addin-dev-certs install && npx office-addin-debugging start manifest.xml",
    "dev": "npx http-server . -p 3000 -S -C ~/.office-addin-dev-certs/localhost.crt -K ~/.office-addin-dev-certs/localhost.key --cors",
    "certs": "npx office-addin-dev-certs install",
    "validate": "npx office-addin-manifest validate manifest.xml",
    "deploy": "cd .. && git subtree split --prefix=outlook-addin -b addin-deploy && git push addin-origin addin-deploy:main --force && git branch -D addin-deploy"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "office-addin-debugging": "^6.1.1",
    "office-addin-dev-certs": "^1.13.0",
    "office-addin-manifest": "^1.13.0"
  }
}
